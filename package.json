{
  "name": "mimo-openai-bridge",
  "version": "1.0.0",
  "description": "Xiaomi MiMo Studio to OpenAI-compatible bridge with futuristic UI",
  "type": "module",
  "main": "src/server.js",
  "packageManager": "npm@11.7.0",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/state.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "2.8.6",
    "express": "5.2.1",
    "multer": "2.1.1"
  }
}
