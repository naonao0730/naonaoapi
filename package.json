{
  "name": "mimo-openai-bridge",
  "version": "1.0.0",
  "description": "Xiaomi MiMo Studio to OpenAI-compatible bridge with futuristic UI",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "latest",
    "express": "latest",
    "multer": "latest"
  }
}
